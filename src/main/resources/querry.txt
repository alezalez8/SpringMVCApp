insert into person (name, age, email)
values ('Nike', 28, 'nike@gmail.com');
insert into person (name, age, email)
values ('Katy', 32, 'katy@gmail.com');
insert into person (name, age, email)
values ('Bod', 52, 'bob@gmail.com');
insert into person (name, age, email)
values ('Aleks', 49, 'aleks@gmail.com');
insert into person (name, age, email)
values ('Lena', 48, 'lena@gmail.com');

select *
from Person;
create sequence first_sequence;

select nextval('first_sequence');

drop sequence first_sequence;

create table Person
(
    id    int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name  varchar(20) NOT NULL,
    age   int CHECK ( age > 0 ),
    email varchar(30) UNIQUE

);

select *
from Person;

create table Person_test
(
    user_id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name    varchar,
    age     int
);

create table Order_test
(
    order_id  int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    item_name varchar,
    user_id   int references Person_test (user_id)
);

drop table Order_test;

INSERT INTO Person_test (name, age)
VALUES ('Nick', 30);
INSERT INTO Person_test (name, age)
VALUES ('Null', 35);

INSERT INTO Order_test (item_name, user_id)
VALUES ('Book', 1);
INSERT INTO Order_test (item_name, user_id)
VALUES ('Airpods', 2);
INSERT INTO Order_test (item_name, user_id)
VALUES ('Iphone', 1);
INSERT INTO Order_test (item_name, user_id)
VALUES ('Kindle', 3);

select * from Person_test join Order_test on Person_test.user_id = Order_test.user_id;

select * from Person_test left join  Order_test on Person_test.user_id = Order_test.user_id;







